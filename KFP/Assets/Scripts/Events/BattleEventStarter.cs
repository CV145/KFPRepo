using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//events related to battle events such as defeating all the enemies to continue
public class BattleEventStarter : EventStarter
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
